[project]
name = "uips-decisions-docs"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "nikola[extras]>=8.3",
]

[dependency-groups]
dev = [
    "ruff",
]

[tool.ruff]
line-length = 100
